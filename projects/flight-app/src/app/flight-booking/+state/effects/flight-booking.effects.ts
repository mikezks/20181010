import { Injectable } from '@angular/core';
/*
import { Actions, Effect, ofType } from '@ngrx/effects';
import { FlightBookingActionTypes } from '../actions/flight-booking.actions';
*/

@Injectable()
export class FlightBookingEffects {
/*
  @Effect()
  loadFoos$ = this.actions$.pipe(ofType(FlightBookingActionTypes.LoadFlightBookings));

  constructor(private actions$: Actions) {}*/
}
